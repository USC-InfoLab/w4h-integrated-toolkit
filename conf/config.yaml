database_number: 1
mapping:
  columns:
    timestamp: timestamp
    user_id: user_id
    value: value
  tables:
    geo:
    - locations
    time_series:
    - heart_rates
    - calories
    - mets
    - distances
    - steps
    - sleep
    - weight
    user_table:
      columns:
        device: String(50)
        location: String(50)
        user_id: String(50)
      name: geomts_users
database1:
  nickname: 'local db'
  dbms: 'postgresql'
  host: 'db'
  port: 5432
  user: 'admin'
  password: 'admin'